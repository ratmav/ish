name: ish ci
on: [push]
jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - name: check out
        uses: actions/checkout@v4
      - name: test
        run: |
          sudo apt-get install -y netcat && cd ${{ github.workspace }} && ./bin/test
      - name: lint
        uses: ludeeus/action-shellcheck@master
        with:
          check_together: 'yes'
          scandir: './src'
