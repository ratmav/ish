name: test
on: [push]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: check out
        uses: actions/checkout@v4
      - name: initialize
        run: |
          cd ${{ github.workspace }} && git submodule init
      - name: test
        run: |
          cd ${{ github.workspace }} && ./bin/test
